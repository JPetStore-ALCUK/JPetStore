<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.mybatis.jpetstore.mapper.SupportMapper">
    <cache />
    <select id="getNewSupportId" resultType="int">
        SELECT COUNT(*) FROM Support;
    </select>
    <insert id="insertSupport">
        INSERT INTO Support (supportId, supportItemId, category, creditcard, exprdate, cardtype, billtofirstname, billtolastname, amount)
        VALUES(#{supportId}, #{supportItemId}, #{supportCategory}, #{creditCard}, #{expiryDate}, #{cardType}, #{billToFirstName}, #{billToLastName}, #{amount})
    </insert>
    <update id="updateSupport">
        UPDATE adoptitem SET supportamount= supportamount + #{amount} WHERE itemid=#{supportItemId}
    </update>



</mapper>