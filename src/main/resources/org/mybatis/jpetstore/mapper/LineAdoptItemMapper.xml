<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.mybatis.jpetstore.mapper.LineItemMapper">
    <cache />

    <select id="getLineAdoptItemsByOrderId" resultType="LineItem">
        SELECT
            orderid,
            lineAdoptNum AS lineAdoptNum,
            adoptid,
            unitprice
        FROM LineAdoptItem
        WHERE orderid = #{orderId}
    </select>

    <insert id="insertAdoptLineItem">
        INSERT INTO LineAdoptItem (orderid, lineAdoptNum, adoptid, unitprice)
        VALUES (#{orderId}, #{lineAdoptNum}, #{adoptid}, #{unitPrice})
    </insert>

</mapper>
